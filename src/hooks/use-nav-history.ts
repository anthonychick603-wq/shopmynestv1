// v1.0.168 — Retired. See src/utils/nav-history.ts for the rationale.
// Kept as an exported no-op so the single mount site in app/_layout.tsx
// keeps compiling until it's removed in a follow-up.
export function useNavHistory(): void {}
